#!/bin/bash
# SAOIS CLI - Executable wrapper (same entry as `saois` after pip install)
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR"
exec python3 -m saois.simple_cli "$@"
